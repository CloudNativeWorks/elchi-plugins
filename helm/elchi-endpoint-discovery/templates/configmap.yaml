apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "elchi-endpoint-discovery.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "elchi-endpoint-discovery.labels" . | nindent 4 }}
data:
  config.yaml: |
    elchi:
      token: {{ .Values.config.token | quote }}
    
    log:
      level: {{ .Values.config.log.level | quote }}
      format: {{ .Values.config.log.format | quote }}
      output: {{ .Values.config.log.output | quote }}
    
    {{- if .Values.config.namespace }}
    namespace: {{ .Values.config.namespace | quote }}
    {{- end }}