{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "elchi-endpoint-discovery.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "elchi-endpoint-discovery.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: true
{{- end }}